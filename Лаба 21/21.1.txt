#include <string>
#include <iostream>
#include <windows.h>
using namespace std;
int main()
{
	SetConsoleCP(1251);
	SetConsoleOutputCP(1251);
	char name[12];
	int k, m, i;
	string s;
	k = 1;
	cout << "Введите строку - ";
	getline(cin, s);
	m = s.size();
	for (i = 1; i < m; i++) {
		if ((s[i] == ' ') & (s[i - 1] != ' ')) {
			k++;
		}
	}
	cout << "Количество слов в строке - " << k;

	return 0;

}